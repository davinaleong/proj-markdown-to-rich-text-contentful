const WORDS = `words`
const CHARACTERS = `characters`
const SENTENCES = `sentences`
const PARAGRAPHS = `paragraphs`

const stringsLib = {
  WORDS,
  CHARACTERS,
  SENTENCES,
  PARAGRAPHS,
}

module.export = stringsLib
